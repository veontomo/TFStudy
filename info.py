import pip
print(pip.pep425tags.get_supported())

# from the Anaconda, select the environment (tensorflow), find package "scipy" among "All", install it.
# It should install scipy and numpy.
# Then, pip install keras
# That's all!